%=============================================================%
%====================== Configuration ========================%
%=============================================================%

fs = 200; % 100 Hz

fc = [55; 65]; % 14 Hz
BW = 0.5; % 2 Hz

%=============================================================%
%====================== Initialization =======================%
%=============================================================%

f = FilterFIR('BandReject', fs, fc, BW);
f.windowType = 'blackman'
H = f.getFrequencyResponse();
h = f.getImpulseResponse();
plot(linspace(0, fs/2, floor(size(H,2) / 2 + 1)), abs(H(1 : (size(H,2) + 1) / 2)))
